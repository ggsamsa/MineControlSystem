project MineControlSystem is

   for Source_Dirs use (".\**");
   for Main use ("main.adb");

   package Pretty_Printer is
   end Pretty_Printer;

   package Builder is
      for Default_Switches ("ada") use ("-gnatwl");
   end Builder;

   package Compiler is
      for Default_Switches ("ada") use ("-g");
   end Compiler;

end MineControlSystem;

